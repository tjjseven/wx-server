$(function () {
    var bodyHeight = window.innerHeight
    var bodyWidth = window.innerWidth
    console.log(bodyWidth)
    $('.body, .anim_body, .touch_box').width(bodyWidth).height(bodyHeight)
    // $('.anim_body').width(bodyWidth).height(bodyHeight)
    // $('.anim_body').width(bodyWidth).height(bodyHeight)
})